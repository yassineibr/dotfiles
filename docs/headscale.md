# Headscale

## Create New User

```bash
sudo headscale users create <USER>
```

## Connect new Device

```bash
# In Client
sudo tailscale login --login-server <HEADSCALE_DOMAIN> --operator=$USER

# In Server
```
